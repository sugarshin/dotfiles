# yazi.toml
# https://yazi-rs.github.io/docs/configuration/yazi
#
# プラグイン本体は package.toml で宣言し、ここで previewer/preloader として有効化する。

[plugin]
prepend_previewers = [
  { url = "*.csv", run = "duckdb" },
]

prepend_preloaders = [
  { url = "*.csv", run = "duckdb" },
]
